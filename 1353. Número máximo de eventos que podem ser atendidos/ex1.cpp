#include <iostream>
#include <vector>
#include <algorithm> // for std::sort
using namespace std;


class Solution {
public:
    int maxEvents(vector<vector<int>>& events) {
        sort(events.begin(), events.end());


    return 1;
        
    }
};

int main() {

    
    /*sort(events.begin(), events.end());
    for(auto& n : events){
        cout << n[0] << " " << n[1] << endl;
    }*/
    
    //CMD não fechar
    int x; cin >> x; 
    return 0;
}